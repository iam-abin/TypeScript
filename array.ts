const nums: number[] = [2, 4, 6, 8];

const heros: string[] = ["thor", "spider man", "hulk"];

const items:(string | number)[] = ["bag","pen",1000,"bottle",500]

heros.map((hero): string => {
    return `hero is ${hero}`
});
